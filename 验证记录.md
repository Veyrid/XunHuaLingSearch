# 验证记录

本版验证日期：2026-09-13。

## 后续无用文件清理（2026-09-13）

- 清除可重新安装的 `node_modules` 构建依赖：61 个普通文件，共 6,096,469 字节，以及项目内部的 `opencc-js` 链接。删除前核实链接目标位于本项目 `node_modules/.pnpm` 内，先删除链接，再清除依赖目录。
- 删除已完成使命的 `scripts/index-metadata.cjs`（1,124 字节）。正式 `build-data.cjs` 已自动生成同样的朝代摘要。当前词库没有未引用的数据文件；1,583 个数据分块全部保留。
- 移除旧界面没有引用的 `inline-field`、`round-status`、`sr-only` 共 5 条样式规则、重复的隐藏规则和未使用的颜色变量，减少 408 字节；移除 Python 测试中的未使用导入。
- 保留数据重建、配对、全文校验、性能验证、可选预览脚本和测试，以及全部来源、整理记录、许可与锁定文件。README 已补充重建前执行 `pnpm install --frozen-lockfile`，日常离线打开无需恢复依赖。
- 删除后执行 39 项 Node 功能测试、6 项 Python 数据准备测试，全部通过；Python 使用 `-B`，未生成新的字节码缓存。静态资源引用、网页脚本语法、全部数据文件存在性及许可文件检查通过。单独验证本地 `dist/vendor/opencc.js` 在没有 `node_modules` 时仍可将“清風明月”转换成“清风明月”。未改动诗句数据或搜索逻辑，未重新执行全库逐条内容扫描或浏览器交互检查。

## 朝代筛选、排序与基础提速（2026-09-13）

- `node --test tests/search.test.cjs tests/pairing.test.cjs tests/catalog.test.cjs`：**39 项通过**。新增 12 项测试涵盖多选朝代、过渡标签、多出处筛选、稳定排序、条件收紧复用、放宽恢复出处、反馈快照、模式切换、中断与失败处理、缓存上限，以及复用结果与独立扫描的一致性。
- `node scripts/validate-data.cjs`：全库检查通过。除已有 29 组诗句案例、1,583 个数据文件的内容与引用检查之外，逐个核对 837 个上下句分块的宽松／严格朝代摘要；25 个原始朝代标签均有显式对应规则。诗文正文、总作品数和配对记录数不变。
- 最终网页脚本与构建脚本语法检查通过；网页以普通本地 script 标签加载新增模块，保持离线打开方式。WebMCP 同步支持 `dynasties` 多选，省略或空数组表示不限，非法选项在修改现有条件前拒绝。
- 分块读取调整为先复用当前缓存、每批最多 3 个；完整候选复用上限为 75,000 个结果和 150,000 个出处。新条件到来时立即让旧查询失效；不保存中断或失败的部分结果。完整反馈的二维格子数组独立拷贝，避免用户改色时影响旧查询判断。
- `node scripts/benchmark-search.cjs` 在真实词库上通过：五言包含“明月”得到 7,213 个候选，继续指定上句第 4 字为“月”得到 954 个；**新增文件读取为 0，所有候选及出处与独立重新扫描完全一致**。
- 同一次命令行测量：首次五言“明月”检索约 5.25 秒；上述追加条件约 9 毫秒，独立重扫同一条件约 4.15 秒。清代七言包含“浩荡离愁”约 1.35 秒，得到唯一去重结果“浩荡离愁白日斜／吟鞭东指即天涯”。这些是 Node 命令行测量，排除了浏览器脚本加载、绘制等开销，不能作为各设备的浏览器速度承诺。

| 严格模式范围 | 不限朝代的文件数 / 大小 | 限定朝代的文件数 / 大小 |
| --- | --- | --- |
| 五言 · 汉 | 194 / 202.27 MB | 4 / 4.07 MB |
| 五言 · 唐 | 194 / 202.27 MB | 22 / 27.11 MB |
| 七言 · 唐 | 193 / 263.13 MB | 12 / 18.73 MB |
| 七言 · 清 | 193 / 263.13 MB | 30 / 35.22 MB |

大小按文件字节数计算（1 MB = 1,000,000 字节），包含混合朝代分块中用于精确筛选的其他记录。本次预览入口返回 HTTP 200；未进行新的浏览器交互或截图测试。初次不限朝代的扫描仍需读取较大词库。

## 数据扩充与清理（2026-09-13，朝代功能之前）

- `node --test tests/search.test.cjs tests/pairing.test.cjs`：27 项通过。覆盖上下句独立定位、整副重复次数、灰黄绿反馈、配对边界、领字、叠句、缺字屏障及先选模式再去重；新增补充来源 ASCII 缺字符号与词体处理。
- `python tests/prepare-data.test.py`：6 项通过。覆盖数据白名单、句号分段、缺字保留、词牌推断、完整组诗覆盖判定、原文哈希保护，以及准备中断后清除旧完成标记，避免残缺缓存被当作完整词库重建。
- 词库生成完毕并清理后，重新执行 `node scripts/validate-data.cjs`，返回 `valid: true`。检查全部 **837 个上下句分块、746 个全文分块**，共 **745,625 首作品、3,580,312 条严格来源记录、6,786,179 条宽松来源记录**。全部字数、原文/简体字符长度、严格证据标记、全文引用与汇总计数有效；网页脚本语法及本地资源引用检查通过。
- 全库 **29 组**实际上下句案例通过。新增“浩荡离愁白日斜／吟鞭东指即天涯”“落红不是无情物／化作春泥更护花”、汉乐府、魏晋诗、北朝乐府等；上述龚自珍两联各有 2 条来源记录，搜索结果按文字合并。
- 《古诗十九首》19 首独立作品均存在，朝代标记为“汉”，完全重复的整组记录已移除。《孔雀东南飞》保留 11 处独句，严格模式恢复 **173 副**上下句；开篇、中段与结尾例句均通过。
- 既有配对回归通过：“千树万树梨花开／散入珠帘湿罗幕”严格 **0** 条、宽松 **3** 条，两副正确原联均保留；词中的不等长句、领字和“知否／知否”仍可查。“床前明月光／疑是地上霜”在引擎去重检查中为 1 个结果、2 个出处。
- 清理 `.cache` 的下载包、解压数据、准备中间数据及未完成下载残片，共 **777,360,346 字节**；清理未被当前清单引用的 885 个旧版分块，共 **390,062,370 字节**；清理 Python 字节码 **14,980 字节**。合计释放 **1,167,437,696 字节**（约 1.17 GB / 1.09 GiB）。删除前核实所有目标在项目目录内且不含链接；保留当前清单引用的 1,583 个数据分块。
- 保留数据源版本锁、23 项整理记录、两套数据许可、构建源码与依赖；清理缓存不影响日常离线打开网页。重建说明已补入 README，并明确 Python 3.11+ 要求。
- 本机预览入口返回 HTTP 200。本版未重新执行浏览器交互或截图检查；以下为上一版的历史浏览器验证记录，不能视为扩充后的浏览器复测。

## 上一版验证记录（2026-09-12）

- `node --test tests/search.test.cjs tests/pairing.test.cjs`：25 项通过。14 项原有条件/反馈测试，加上 11 项配对测试，覆盖跨联排除、多联同段、非标准标点、词中不等长句与领字、叠句、三分句歧义、缺字/空行屏障、冒号引语、Unicode 以及先选模式再合并出处。
- `node scripts/validate-data.cjs`：检查全部 552 个上下句分块及 333 个全文分块；共 332,037 首作品，严格配对来源记录 1,439,044 条，宽松共 2,671,552 条。所有引用、字数、严格依据标记及汇总计数有效，原文/简体字符长度一致。网页脚本语法检查通过。
- 全库 15 组实际案例验证通过：包含《白雪歌》《雨霖铃》《如梦令》《水调歌头》《声声慢》《诗经》等。跨联“千树万树梨花开／散入珠帘湿罗幕”严格为 0 条、宽松有 3 个来源；两副正确原联均保留。另验证“在河之洲／窈窕淑女”只在宽松中出现。
- 当时实际浏览器：上述跨联组合在严格模式无结果；点击空结果的“保留条件，试试所有相邻句”，字数、包含和定位条件保留，宽松模式显示 1 个去重结果、3 个出处。切回严格并查询“念去去千里烟波／暮霭沈沈楚天阔”得到唯一结果，全文保留顿号并高亮完整句。
- 实际浏览器：`床前明月光／疑是地上霜` 用上下句不同位置及跨句排位同时筛出，显示 1 条结果、2 个出处；两个全文来源可查看。冲突的 WebMCP 输入会拒绝且不破坏原筛选状态。
- 实际反馈录入：输入 `关关关关／在河之洲`，上句标记绿、绿、灰、灰，下句全部绿色，得到唯一候选 `关关雎鸠／在河之洲`。验证灰色重复字不会误将已有的同字全部排除。
- 当时实际浏览器 1280px 宽屏及 390px 窄屏检查：配对选择框、说明与结果显示正常，没有横向溢出，控制台未出现错误。测试结束恢复浏览器原始尺寸。
- 纯静态交付：所有脚本、样式、词库与 OpenCC 均在本地，数据通过普通 script 标签加载，不使用 fetch 或 ES module 读取本地文件。内置测试浏览器的安全策略禁止访问 `file:` URL，因此实际浏览器功能检查通过本机 HTTP 预览完成；没有绕过该限制。双击入口已做静态路径及依赖完整性检查。

当时的离线版本不请求远端数据。外部来源链接仅在用户点击时联网；在线发布行为见下方 GitHub Pages 记录。

严格配对是基于数据源段落、标点和诗行结构的确定性推断，以上验证不意味着所有古体诗或词的语义断句都准确。有歧义的词中平行句和分片可能被严格模式省略，可保留条件切换宽松模式并核对全文。


## 2026-09-13 · GitHub Pages 发布准备

- 添加 GitHub Actions 工作流：所有分支及 PR 检查，仅默认分支上传并发布 dist；不安装依赖、不下载或重建词库。
- 页面改为明确点击“开始检索”；打开、编辑、清空或填入示例都不会触发大词库读取。全文按点击题名读取。
- 项目自身按用户决定不添加 LICENSE；两套数据和 OpenCC 的现有许可完整保留，在线说明同步更新。
- 41 项 JavaScript 测试、6 项 Python 测试通过。全库检查通过：745,625 首、6,786,179 条宽松记录、3,580,312 条严格记录；29 组代表诗例及去重验证通过。
- 发布检查通过：1,598 个站点文件，约 817.5 MB，未超过检查器使用的 1,000,000,000 字节上限。
- 浏览器通过 http://127.0.0.1:5173/XunHuaLingSearch/ 检查初始待检索页面；选择清代、七言并输入“浩荡离愁”，手动检索返回一副结果、两个出处，全文正常显示《己亥杂诗 其五》。
- 测试使用本机 HTTP 模拟项目子路径，不等同于 GitHub 线上部署验证。

- 上传前隐私检查：扫描全部待提交目录中的常见私钥、访问令牌、密码赋值、邮箱、本机路径与用户名、私网地址、含凭据 URL 和手机号模式；未发现敏感项。两处路径匹配均为公开来源链接中的 /Home/，已人工复核。第三方公开版权声明原样保留；Git 作者与提交者均使用 GitHub 用户名和 noreply 邮箱。此项为模式扫描及人工复核，不保证识别所有非标准或编码后的秘密。
